# ─────────────────────────────────────────────────────────────────────────────
# Cópia local APENAS: cp .env.example .env
# NÃO commitar .env — deploy automático no Vercel lê o painel Environment.
# ─────────────────────────────────────────────────────────────────────────────

# ── Produção (painel Vercel) ──────────────────────────────────────────────────
# VITE_API_BASE_URL=https://sua-api.com/api
# VITE_DEMO_MODE=false

# ── Desenvolvimento local com a nova arquitetura (React -> BFF -> FastAPI) ────
VITE_API_BASE_URL=/api
VITE_DEMO_MODE=false

